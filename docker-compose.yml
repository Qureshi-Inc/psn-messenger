services:
  psn-messenger:
    build: .
    environment:
      - NPSSO_TOKEN=${NPSSO_TOKEN}
      - GROUP_ID=${GROUP_ID}
    ports:
      - "3001:3000"
    volumes:
      - psn_data:/data
    restart: unless-stopped

volumes:
  psn_data:
